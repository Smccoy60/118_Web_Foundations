# README.md
Purpose:
The purpose of this site is to eventually use it as a resouce hub to keep everything in one place.  This site will also be used to give information to employee as well as helpful links.

Code Explanation: 
The first initial iteration is not expanding the header color all the way across the entire screen when viewing from different devices.  I think I probably need to add a flex option in the code to make this work. 
I added a class called aside container.  This allowed me to add this class into the css stylesheet, where I put in the flex wrap and added a gap between the two aside sections.
I then added two aside sections where I changed the background color on the css stylesheet and added the flex, 1.  This allows both of the sections to take up the same amount of space in that part of the grid.  If I change one to a flex 2 and leave one at flex 1, then one box would take up twice as much space across as the other. 

One problem that I still need to improve is when viewing the web page on the different size screens, the words in the Why Payroll Matters sections go beyond the colored background.  